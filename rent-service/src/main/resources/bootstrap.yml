#spring:
#  application:
#    name: rent
#server:
#  port: 8181
#eureka:
#   client:
#      register-with-eureka: true
#      fetch-registery: true
      
#########################################      
spring:
  application:
    name: rent
server:
  port: 8191
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
  instance:
    instance-id: ${spring.application.name}-${random.int}
    hostname: localhost

