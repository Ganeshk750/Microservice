#spring:
#  application:
#    name: vehicle
#server:
#  port: 9191
#eureka:
#   client:
#      register-with-eureka: true
#      fetch-registery: true

      
      
server:
  port: 0
spring:
  application:
    name: vehicle
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
  instance:
    instance-id: ${spring.application.name}-${random.int}
    hostname: localhost